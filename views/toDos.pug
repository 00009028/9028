extends layout.pug 

block content 
    div.container
        h1 All to-dos

        each toDo in toDos
            div.to-do-wrapper
                a.to-do(href=`/toDos/${ toDo.id }`) #{ toDo.title }
                a.delete(href='#') delete